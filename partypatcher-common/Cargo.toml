[package]
name = "partypatcher-common"
version = "1.0.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2"
partymod-common = { path = "../partymod-common" }
